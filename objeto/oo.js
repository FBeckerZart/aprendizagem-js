// CÓDIGO NÃO EXECUTÁVEL!!!

// Procedural
processamento(valor1, valor2, valor3)

// OO
objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        // ...
    }
}

objeto.processamento() // Foco passou a ser o objeto

// Principios importantes
// 1. Abstração -> pegar objetos da vida real e transformar em código.
// 2. Encapsulamento -> faz com que somente as interfaces de comunicação com o sistema sejam mostradas, detalhes menores e mais específicos estão encapsulados.
// 3. Herança (prototype) 
// 4. Polimorfismo -> multiplas formas